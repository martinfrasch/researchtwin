version: '3.8'

services:
  backend:
    build: ./backend
    container_name: researchtwin_backend
    volumes:
      - ./backend/data:/app/data
    environment:
      - PERPLEXITY_API_KEY=${PERPLEXITY_API_KEY}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - CACHE_DIR=/app/data/cache
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - PYTHONUNBUFFERED=1
    expose:
      - "8000"
    restart: unless-stopped
    networks:
      - researchtwin-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  discord_bot:
    build: ./backend
    container_name: researchtwin_discord_bot
    # Disabled by default: requires DISCORD_BOT_TOKEN (and DISCORD_GUILD_ID) in .env.
    # Without a token the bot crash-loops. To re-enable, set the token in .env and run:
    #   docker compose --profile discord up -d discord_bot
    profiles: ["discord"]
    command: python discord_bot.py
    depends_on:
      backend:
        condition: service_healthy
    environment:
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - DISCORD_GUILD_ID=${DISCORD_GUILD_ID}
      - API_BASE_URL=http://backend:8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - researchtwin-network

networks:
  researchtwin-network:
    name: researchtwin-network
    driver: bridge
